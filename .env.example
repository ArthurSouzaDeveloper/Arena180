# Usado pelo docker-compose.yml na raiz do projeto.
POSTGRES_USER=gestquadra
POSTGRES_PASSWORD=troque-por-uma-senha-forte
POSTGRES_DB=gestquadra

# Gere com: openssl rand -base64 48
JWT_ACCESS_SECRET=troque-por-um-segredo-forte

# Origem pública do frontend (protocolo + host + porta, sem barra no final)
CORS_ORIGIN=http://SEU_IP_OU_DOMINIO:8082
